# Get a specific media file by ID (User / Driver / Admin)
query GetMedia {
  media(mediaId: "media-001") {
    success
    statusCode
    message
    timeStamp
    data {
      mediaId
      ownerId
      fileName
      contentType
      mediaType
      size
      status
      objectKey
      createdAt
      updatedAt
      versions {
        versionType
        objectKey
        contentType
        size
        width
        height
        durationMs
      }
    }
  }
}

# Get paginated list of media files for owner (User / Driver / Admin)
query ListMedia {
  listMedia(ownerId: "user-001", limit: 10, cursor: "") {
    success
    statusCode
    message
    timeStamp
    data {
      paginationInfo {
        totalItems
        currentPage
        nextPage
      }
      items {
        mediaId
        ownerId
        fileName
        contentType
        mediaType
        size
        status
        createdAt
      }
    }
  }
}

# Get upload status (User / Driver / Admin)
query GetUploadStatus {
  uploadStatus(uploadId: "upload-001") {
    success
    statusCode
    message
    timeStamp
    data {
      uploadId
      status
      totalParts
      completedParts
      missingParts
      expiresAt
    }
  }
}

# Get presigned download URL (User / Driver / Admin)
query GetDownloadUrl {
  downloadUrl(
    mediaId: "media-001"
    versionType: "original"
    expirySeconds: 3600
  ) {
    success
    statusCode
    message
    timeStamp
    data {
      url
      expiresAt
      contentType
    }
  }
}

# Get user quota (User / Driver / Admin)
query GetQuota {
  quota {
    success
    statusCode
    message
    timeStamp
    data {
      usedBytes
      quotaBytes
      remainingBytes
      activeUploads
      maxConcurrentUploads
    }
  }
}

# Get DLQ topics (Admin)
query GetDLQTopics {
  dlqTopics
}

# Get DLQ statistics (Admin)
query GetDLQStats {
  dlqStats(topics: ["media-events-dlq"]) {
    success
    statusCode
    message
    timeStamp
    data {
      topic
      messageCount
    }
  }
}

# Create upload session (User / Driver / Admin)
mutation CreateUploadSession {
  createUploadSession(
    input: {
      fileName: "sample.mp4"
      contentType: "video/mp4"
      size: 104857600
      checksum: "sha256-xyz"
      deliveryId: "delivery-123"
      idempotencyKey: "upload-key-123"
    }
  ) {
    success
    statusCode
    message
    timeStamp
    data {
      mediaId
      uploadId
      s3UploadId
      presignedParts {
        partNumber
        presignedUrl
      }
      partSize
      totalParts
      expiresAt
    }
  }
}

# Complete upload session (User / Driver / Admin)
mutation CompleteUpload {
  completeUpload(
    input: {
      uploadId: "upload-001"
      parts: [{ partNumber: 1, eTag: "etag-001" }]
      idempotencyKey: "complete-key-123"
    }
  ) {
    success
    statusCode
    message
    timeStamp
    data {
      mediaId
      status
    }
  }
}

# Abort upload session (User / Driver / Admin)
mutation AbortUpload {
  abortUpload(uploadId: "upload-001") {
    success
    statusCode
    message
    timeStamp
    data
  }
}

# Delete media file (User / Driver / Admin)
mutation DeleteMedia {
  deleteMedia(mediaId: "media-001", idempotencyKey: "del-001") {
    success
    statusCode
    message
    timeStamp
    data
  }
}

# Replay DLQ messages (Admin)
mutation ReplayDLQ {
  dlqReplay(topic: "media-events-dlq", maxMessages: 50) {
    success
    statusCode
    message
    timeStamp
    data {
      success
      replayedCount
      errors
    }
  }
}

# Renew presigned upload URLs (User / Driver / Admin)
mutation RenewPresigned {
  renewPresigned(input: { uploadId: "upload-001", expirySeconds: 3600 }) {
    success
    statusCode
    message
    timeStamp
    data {
      uploadId
      s3UploadId
      presignedParts {
        partNumber
        presignedUrl
      }
      partSize
      totalParts
      expiresAt
    }
  }
}
