# Get Payment Service Info (Public)
query GetPaymentServiceInfo {
  paymentServiceInfo {
    success
    statusCode
    message
    timeStamp
    data {
      name
      version
      status
    }
  }
}

# Get Payment by ID with federated User & Delivery stubs (User / Driver / Admin)
query GetPaymentById {
  payment(id: "12345678-1234-1234-1234-123456789abc") {
    success
    statusCode
    message
    timeStamp
    data {
      id
      deliveryId
      delivery {
        id
      }
      userId
      user {
        id
      }
      amountMinor
      currency
      status
      provider
      providerPaymentId
      correlationId
      causationId
      createdAt
      updatedAt
      authorizedAt
      capturedAt
    }
  }
}

# List Payments - Paginated with paginationInfo (Admin)
query GetPaymentsList {
  payments(page: 1, limit: 10) {
    success
    statusCode
    message
    timeStamp
    data {
      paginationInfo {
        totalItems
        currentPage
        totalPages
        hasNextPage
        hasPreviousPage
        nextPage
        previousPage
      }
      items {
        id
        deliveryId
        delivery {
          id
        }
        userId
        user {
          id
        }
        amountMinor
        currency
        status
        createdAt
      }
    }
  }
}
